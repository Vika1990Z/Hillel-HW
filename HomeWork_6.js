// Вывести таблицу умножения на 7.

const multiplier = 7;
let result = null;

for (let i=0; i<=10; i++) {
    result=multiplier * i;
    document.write(`${multiplier} * ${i} = ${result};<br \/>`);
}


import './styles.scss'

const Error = () => (
    <main >
        <div className='detailes'>
            <p style={{textAlign:"center"}}>Error, can't reach the page.</p>
        </div>
    </main>
);


export default Error;